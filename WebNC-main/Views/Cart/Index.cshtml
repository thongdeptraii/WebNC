@model QLNhaSach1.Models.Cart

@{
    ViewData["Title"] = "Giỏ hàng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container my-5">
    <h2 class="mb-4"><i class="bi bi-cart3"></i> Giỏ hàng</h2>

    @if (Model == null || !Model.CartItems.Any())
    {
        <div class="alert alert-info text-center">
            Giỏ hàng của bạn đang trống.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th style="width: 80px;">Hình</th>
                        <th>Sách</th>
                        <th class="text-end">Giá</th>
                        <th class="text-center">Số lượng</th>
                        <th class="text-end">Tổng</th>
                        <th class="text-center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.CartItems)
                    {
                        <tr>
                            <td>
                                <img src="@Url.Content(item.Book.ImageUrl)" alt="@item.Book.bookName" class="img-thumbnail"
                                    style="height: 70px; object-fit: cover;" />
                            </td>
                            <td>@item.Book.bookName</td>
                            <td class="text-end">@item.Book.price.ToString("N0") VNĐ</td>
                            <td class="text-center">
                                <form asp-action="UpdateCart" method="post" class="quantity-form">
                                    <input type="hidden" name="cartItemId" value="@item.CartItemId" />
                                    <input type="number" name="quantity" value="@item.Quantity" min="1"
                                        class="form-control form-control-sm quantity-input" />
                                    <button type="submit" class="btn btn-outline-secondary btn-sm btn-update">✔</button>
                                </form>
                            </td>
                            <td class="text-end">@((item.Book.price * item.Quantity).ToString("N0")) VNĐ</td>
                            <td class="text-center">
                                <form asp-action="RemoveFromCart" method="post"
                                    onsubmit="return confirm('Xác nhận xóa sách này khỏi giỏ hàng?');" class="d-inline">
                                    <!-- Thêm class d-inline để form không chiếm full width -->
                                    <input type="hidden" name="cartItemId" value="@item.CartItemId" />
                                    <button type="submit" class="btn btn-outline-danger btn-sm btn-delete">🗑</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                    <tr class="fw-bold">
                        <td colspan="4" class="text-end">Tổng cộng:</td>
                        <td class="text-end text-primary">@Model.CartItems.Sum(ci => ci.Book.price *
                                                    ci.Quantity).ToString("N0") VNĐ</td>
                    <td></td>
                </tr>
            </tfoot>
        </table>
    </div>

        <div class="text-end mt-3">
            <a href="@Url.Action("Checkout", "Order")" class="btn btn-primary px-4">Thanh toán</a>
        </div>
        }
</div>
